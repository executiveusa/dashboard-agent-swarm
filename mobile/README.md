# Mobile (Expo) Client

The mobile workspace will deliver an Expo application targeting iOS and Android devices. It should reuse the shared SDK for
command streaming, provide push notification support, microphone capture, and offline command queueing.
