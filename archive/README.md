# Archived packages

The contents of this folder are legacy experiments that are no longer part of the core YouTube → Transcript/QA → Report workflow.  They are preserved for reference but excluded from the active build.  Nothing inside `archive/` is loaded at runtime.
