# Lovable Cloud Backend

This workspace encapsulates the Lovable Cloud Functions, migrations, and shared services (memory APIs, integration webhooks,
workflow triggers). It should export scripts to run the edge runtime locally, along with Docker assets for Coolify deployments.
