# Next.js Application Shell

The `app` workspace will host the Next.js (App Router) frontend powered by the shared Shadcn design system. It should consume the
shared SDK for interacting with the Lovable Cloud backend, expose streaming chat, workflow management, integration toggles, and
observability dashboards.
