# SDK Package

Provides typed clients for interacting with the backend APIs (REST/GraphQL), command streaming hooks, and workflow helpers used by the web, desktop, and mobile applications.

## Current Modules

- `a2a/envelope`: Zod schema + helpers for the Agent-to-Agent envelope, mirroring the JSON schema used by transports.
